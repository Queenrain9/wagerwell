window.WAGERWELL_PROVIDER = {
  // "sim" keeps the built-in WAGERWELL prototypes active.
  // Switch to "evolution" only after the server is deployed and Evolution OSS credentials are configured.
  mode: "sim",
  backendBase: "",
  launchMode: "iframe"
};
